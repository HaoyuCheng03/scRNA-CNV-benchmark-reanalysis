## Scripts

- **`auc_f1.R`** – Evaluates CNV calling performance by computing Pearson correlation, ROC AUC, partial AUC, and F1 scores, and performs statistical comparisons across evaluation settings.

## Input data

- `<sampleID>_combined_methods.rds"` files used in auc_f1.R are stored in `SampleID_combined_methods.zip`.
